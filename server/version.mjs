// Independent protocol/runtime version for the local notebook server.
// Keep this separate from the Chrome extension version: either component can
// be updated on its own, and the popup deliberately shows both.
export const WOLFBOOK_SERVE_VERSION = '0.3.1';
